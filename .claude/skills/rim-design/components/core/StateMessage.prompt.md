Inline feedback message. The ONLY place RIM uses a coloured left border.

```jsx
<StateMessage tone="success" label="Success.">Your changes have been saved.</StateMessage>
```

Copy stays plain and human — "Something needs fixing." not "Error 422".
