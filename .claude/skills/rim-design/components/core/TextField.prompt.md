Calm labelled input — 44px minimum height, 16px text so iOS never zooms.

```jsx
<TextField label="Email" type="email" placeholder="you@example.org" help="We only write when something is happening." />
```

`onDark` gives the translucent white-on-blue treatment used in the site footer newsletter row.
