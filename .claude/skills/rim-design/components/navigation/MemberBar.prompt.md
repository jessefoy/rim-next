Signed-in header. Deliberately near-empty: brand left, avatar + first name + Sign out right.

```jsx
<MemberBar logoSrc="assets/logo/Rooted-In-Mindfulness-Logo.png" name="Marguerite Chen" />
```

Never put page navigation here — that belongs to `AccountSidebar`.
