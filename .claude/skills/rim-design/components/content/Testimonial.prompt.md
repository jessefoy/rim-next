Community voice. Always anonymous — RIM attributes to "Community member", never a full name.

```jsx
<Testimonial>After the first drop-in session, I knew I was home.</Testimonial>
```

Italic sans on `--rim-bg` with a 3px `--rim-rule` top border. No portrait, no card shadow.
