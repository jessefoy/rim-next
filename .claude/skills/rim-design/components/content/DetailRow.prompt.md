Fact row for a program details card — icon, plain sentence, optional trailing link.

```jsx
<DetailRow icon={<CalendarDays size={20} strokeWidth={1.75} />}>Tuesdays, 9:30–10:45 AM</DetailRow>
```

Body text is 17px here (a named exception to the scale, matching `pg-detail-row__text`).
